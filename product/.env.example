# ── 稿子（最要紧的一把 key）─────────────────────────
# 没有它，生成走本地模板：结构对、能跑完，但不是为她写的。
ANTHROPIC_API_KEY=
# 可选，默认 claude-opus-5
# WREN_MODEL=claude-opus-5

# ── 声音 ───────────────────────────────────────────
# 两个填一个就行。都不填时用浏览器内置合成（能听，但像导航播报）。
# 优先级：WREN_TTS_PROVIDER > ElevenLabs > OpenAI > 浏览器
ELEVENLABS_API_KEY=
# ELEVENLABS_VOICE_ID=XrExE9yKIg1WjnnlVkGX
# ELEVENLABS_MODEL_ID=eleven_multilingual_v2

OPENAI_API_KEY=
# OPENAI_TTS_VOICE=shimmer
# OPENAI_TTS_MODEL=gpt-4o-mini-tts

# WREN_TTS_PROVIDER=elevenlabs
